spring:
  profiles:
    active: dev
  application:
    name: seat-locking-service
  config:
    import: "optional:configserver:http://localhost:8888"
server:
  port: 9087
springdoc:
  swagger-ui:
    path: /swagger-ui.html
