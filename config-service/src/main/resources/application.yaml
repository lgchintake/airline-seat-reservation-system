server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lgchintake/airline-seat-reservation-system
          default-label: main
          clone-on-start: true
          search-paths:
            - configs/*